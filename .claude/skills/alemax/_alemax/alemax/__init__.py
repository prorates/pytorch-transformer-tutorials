"""The /alemax:* family.

Developed here as one package with shared code and tests; shipped to projects as a
vendored copy of this tree plus a thin entry point per verb (see ../../build.py).
Nothing in the shipped path may import outside the standard library.
"""

__version__ = "0.2.0"
